*/ There will be three variables in equations, to be able to find them, we need at least 3 equations in the form of ax + by + cz = d .
READ(a1, b1, c1, d1);
READ(a2, b2, c2, d2);
READ(a3, b3, c3, d3);

IF a, b, c == 0
  PRINT(unvalid input);

*/ We should eliminate variables
FIND(k1, l1, m1);
FIND(k2, l2, m2);

FIND(z);
FIND(y);
FIND(x);

PRINT(x, y, z);

